# Phase 5.1 — Production Config Audit

## Audit Date: Runtime of deployment preparation

## Files Audited

| File | Status | Issues |
|------|--------|--------|
| `render.yaml` | ✅ PASS | None |
| `Dockerfile` | ✅ PASS | Minor: tsx reinstall workaround |
| `docker-compose.yml` | ⚠️ NOTES | Not used in production (local Docker only) |
| `docker-compose.prod.yml` | ⚠️ NOTES | References Railway |
| `vercel.json` | ✅ PASS | None |
| `.env.example` | ✅ PASS | Very comprehensive, no issues |
| `config/environments/.env.production.example` | ⚠️ WARN | Title says "RAILWAY BACKUP" |
| `.env.prod` | ⚠️ NOTES | References Qdrant/Ollama (not used in Render) |

## render.yaml — Audit

**Status: ✅ PASS** — Ready for production deployment

| Check | Finding |
|-------|---------|
| Runtime | `node` with `buildCommand` and `startCommand` ✅ |
| Region | `singapore` — closest to India ✅ |
| Plan | `free` ✅ |
| Health check | `/healthz` ✅ |
| Build cache | `npm ci` with frozen lockfile ✅ |
| DB migration | `FORCE_MIGRATIONS=true` ✅ |
| CORS | `EXTRA_ALLOWED_ORIGINS` includes stockstory-india.com ✅ |
| No Railway deps | ✅ — all env vars are Render-native |

**Recommendations:**
1. Add `NODE_OPTIONS=--max-old-space-size=512` for free-plan memory safety
2. Add `TZ=Asia/Kolkata` for proper timezone handling
3. None critical — current config is production-ready

## Dockerfile — Audit

**Status: ✅ PASS** — Multi-stage, no GPU dependencies

| Check | Finding |
|-------|---------|
| Multi-stage build | ✅ Builder + runner stages |
| Layer cache | ✅ `npm ci` before COPY for caching |
| Python 3 | ✅ Installed for NSE data providers |
| No GPU/CUDA | ✅ Clean — no CUDA, no Ollama, no Qdrant |
| Path aliases | Uses `tsx` to resolve `@/` paths ✅ |
| Python venv | ✅ Uses `/opt/venv` for PEP 668 compliance |

**Minor observation:** `RUN npm prune --omit=dev || true` and then `npm install --no-save tsx` — this works but is a bit hacky. The `tsx` dependency should be listed as a non-dev dependency or the build should bundle it.

## vercel.json — Audit

**Status: ✅ PASS** — Minimal, correct

| Check | Finding |
|-------|---------|
| Version | 2 ✅ |
| Output dir | `dist/public` ✅ |
| API rewrites | `https://stockstory-api.onrender.com/api/$1` ✅ |
| SPA fallback | `/(.*) → /index.html` ✅ |

**Recommendation:** Add `"trailingSlash": false` to avoid redirect chains. Not critical.

## .env Files — Audit

### `.env.example` ✅ PASS
- All vars documented with clear sections (REQUIRED / OPTIONAL / LOCAL-ONLY)
- Comments explain how to generate secrets (`openssl rand -base64 64`)
- Firebase keys, API keys all marked as placeholder values

### `config/environments/.env.production.example` ⚠️ WARN
- Title header says "RAILWAY BACKUP" — **should not mention Railway**
- `PORT=4001` conflicts with Render port 10000
- `DATABASE_URL` uses Railway-style `postgres:5432` hostname (won't work on Render)
- `POSTGRES_PASSWORD` references Railway auto-injected env var
- `REDIS_URL` references Railway-style `replace_with_upstash_host`
- Should be renamed or updated to reference Render/Neon

### `.env.prod` ⚠️ NOTES
- References Qdrant, Ollama, SGLang URLs — these are Docker-only services
- Not used in Render production — safe to ignore

## Env Var Inconsistencies

| Variable | `.env.example` | `config/environments/.env.production.example` | Notes |
|----------|---------------|-----------------------------------------------|-------|
| `PORT` | 10000 | 4001 | Conflict — Render uses 10000 |
| `VITE_API_BASE_URL` | `https://www.stockstory-india.com/api` | Not set | Missing in production example |
| `VITE_APP_DOMAIN` | `stockstory-india.com` | Not set | Missing in production example |
| `SGLANG_URL` | Documented as local-only | Documented but not used | Both fine |
| `VERCEL_API_URL` | Not in .env.example | Not in .env.production.example | Set in render.yaml only |

## Summary

### Critical Issues (must fix before launch)
1. **`.env.production` contains real secrets** — database URL with password, Redis tokens, Firebase private key. Must be in `.gitignore`.

### Warnings (should fix)
1. `config/environments/.env.production.example` — still references Railway; should be updated to reference Render+Neon
2. `tsx` dependency workaround in Dockerfile (minor)

### Observations (non-blocking)
1. `docker-compose.prod.yml` mentions Railway in comments
2. `.env.prod` references Ollama/Qdrant (not used in production)
3. No memory limit env var for Node on free plan
4. No timezone env var for market operations
