# F1 Data-Quality Remediation Implementation Report

This branch adds provider normalization, corrected stock-specific scoring directions, non-destructive price quarantine, immutable prediction quarantine, guarded append-only prediction promotion, lineage and completeness writes, expanded regressions, and a dedicated F1 CI dry-run job.

External providers remain disabled until documented or licensed automated access is confirmed. Apply mode remains disabled unless the explicit repair or pipeline confirmation environment variables are set.

Keep the pull request draft until hosted CI and reviewed database-copy dry runs pass.
