import React, { useEffect, useMemo, useState } from "react";
import { ArrowRight, Bell, Eye, GitCompare, Search, Sparkles } from "lucide-react";
import { RecentSearchStore } from "../services/search/RecentSearchStore";

import { WatchlistEngine } from "../services/portfolio/WatchlistEngine";

function navigate(pageKey: string): void { const params = new URLSearchParams(window.location.search); params.set("page", pageKey); params.delete("id"); params.delete("q"); window.history.pushState({}, "", `?${params.toString()}`); window.dispatchEvent(new Event("urlchange")); }
function openCompany(symbol: string): void { RecentSearchStore.addTicker(symbol); const params = new URLSearchParams(window.location.search); params.set("page", "stock"); params.set("id", symbol); window.history.pushState({}, "", `?${params.toString()}`); window.dispatchEvent(new Event("urlchange")); }

const destinations = [
  { title: "Discover", body: "Find companies worth a closer look.", icon: Sparkles, page: "scanner" },
  { title: "Compare", body: "Put two research cases side by side.", icon: GitCompare, page: "compare" },
  { title: "Track", body: "Return to the companies that matter.", icon: Eye, page: "watchlist" },
] as const;

export const DashboardHub: React.FC = () => {
  const [watchlists, setWatchlists] = useState(() => WatchlistEngine.getWatchlists());
  const [recentResearch, setRecentResearch] = useState<string[]>([]);
  useEffect(() => { setRecentResearch(RecentSearchStore.getRecent()); const handler = () => setWatchlists([...WatchlistEngine.getWatchlists()]); window.addEventListener("watchlistchange", handler); return () => window.removeEventListener("watchlistchange", handler); }, []);
  const followedTickers = useMemo(() => { const unique = new Set<string>(); watchlists.forEach((watchlist) => watchlist.tickers.forEach((ticker) => unique.add(ticker))); return [...unique].slice(0, 5); }, [watchlists]);

  return <div className="mx-auto w-full max-w-[1180px] overflow-x-hidden px-4 pb-24 pt-5 sm:px-6 sm:pt-8">
    <section className="rounded-[var(--r-lg)] border border-[var(--c-border)] bg-[var(--c-surface)] p-5 sm:p-8">
      <div className="grid gap-7 lg:grid-cols-[1fr_360px] lg:items-center"><div><div className="flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[0.18em] text-[var(--c-ink-muted)]"><Sparkles className="h-[18px] w-[18px] text-[var(--c-ink-muted)]" /> Your research workspace</div><h1 className="mt-3 max-w-2xl text-[32px] font-semibold leading-[1.15] tracking-[-0.5px] text-[var(--c-ink)]">What company are you thinking about?</h1><p className="mt-3 max-w-xl text-[15px] leading-[1.6] text-[var(--c-ink-secondary)]">Start with one company. We’ll organize the quality, valuation, risk, and thesis context for you.</p><button type="button" onClick={() => navigate("search")} className="mt-6 flex h-12 w-full max-w-xl items-center gap-3 rounded-[var(--r-lg)] border border-[var(--c-border-strong)] bg-[var(--c-surface-sunken)] px-4 text-left text-[14px] text-[var(--c-ink-disabled)] transition-colors hover:border-[var(--c-brand)]"><Search className="h-[18px] w-[18px] text-[var(--c-ink-muted)]" /><span className="flex-1">Search a company or symbol</span><ArrowRight className="h-[14px] w-[14px] text-[var(--c-ink-muted)]" /></button></div>
        <div className="rounded-[var(--r-lg)] border border-[var(--c-border)] bg-white p-5"><div><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-[var(--c-ink-muted)]">A simple path</p><h2 className="mt-2 text-[14px] font-semibold text-[var(--c-ink)]">Search → understand → revisit</h2><p className="mt-2 text-[13px] leading-[1.6] text-[var(--c-ink-secondary)]">No wall of indicators. Just the clearest next step for your research.</p></div><div className="mt-5 flex items-center justify-between">{destinations.map(({ title, icon: Icon }) => <div key={title} className="text-center"><Icon className="mx-auto h-[18px] w-[18px] text-[var(--c-ink-muted)]" /><span className="mt-2 block text-[10px] font-semibold text-[var(--c-ink-secondary)]">{title}</span></div>)}</div></div></div>
    </section>
    <section className="mt-5 grid gap-3 sm:grid-cols-3">{destinations.map(({ title, body, icon: Icon, page }) => <button key={title} type="button" onClick={() => navigate(page)} className="group flex min-h-[116px] items-start gap-4 rounded-[var(--r-lg)] border border-[var(--c-border)] bg-white p-5 text-left transition-colors hover:border-[var(--c-border-strong)]"><Icon className="h-[18px] w-[18px] shrink-0 text-[var(--c-ink-muted)]" /><span className="min-w-0 flex-1"><span className="block text-[14px] font-semibold text-[var(--c-ink)]">{title}</span><span className="mt-1 block text-[13px] leading-[1.6] text-[var(--c-ink-secondary)]">{body}</span></span><ArrowRight className="mt-1 h-[14px] w-[14px] text-[var(--c-ink-disabled)]" /></button>)}</section>
    <section className="mt-5 grid gap-4 lg:grid-cols-[1fr_340px]"><div className="rounded-[var(--r-lg)] border border-[var(--c-border)] bg-white p-5"><div className="flex items-start gap-3"><Bell className="mt-0.5 h-[18px] w-[18px] text-[var(--c-ink-muted)]" /><div><h2 className="text-[14px] font-semibold text-[var(--c-ink)]">What needs attention</h2><p className="mt-0.5 text-[13px] text-[var(--c-ink-secondary)]">Important changes from companies you track will appear here.</p></div></div><button type="button" onClick={() => navigate("alerts")} className="mt-5 inline-flex items-center gap-2 text-xs font-semibold text-[var(--c-brand)]">Review changes <ArrowRight className="h-3.5 w-3.5" /></button></div>
      <div className="rounded-[var(--r-lg)] border border-[var(--c-border)] bg-white p-5"><h2 className="text-[14px] font-semibold text-[var(--c-ink)]">Continue researching</h2>{followedTickers.length === 0 && recentResearch.length === 0 ? <p className="mt-2 text-[13px] leading-[1.6] text-[var(--c-ink-secondary)]">Your recent and tracked companies will collect here after your first search.</p> : <div className="mt-3 flex flex-wrap gap-2">{[...new Set([...followedTickers, ...recentResearch])].slice(0, 6).map((ticker) => { return <button key={ticker} type="button" onClick={() => openCompany(ticker)} className="rounded-[var(--r-sm)] border border-[var(--c-border)] bg-[var(--c-surface-sunken)] px-3 py-2 font-mono text-[11px] font-semibold text-[var(--c-ink-secondary)] hover:border-[var(--c-border-strong)]">{ticker}</button>; })}</div>}</div></section>
  </div>;
};
export default DashboardHub;
