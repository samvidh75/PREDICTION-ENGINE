import React from "react";
import { ArrowRight, BarChart3, BrainCircuit, Database, FileSearch, LineChart, LockKeyhole, Radar, ShieldCheck } from "lucide-react";
import TopNav from "../components/navigation/TopNav";
import MobileNav from "../components/navigation/MobileNav";

function setPage(pageKey: string): void {
  const params = new URLSearchParams(window.location.search);
  params.set("page", pageKey);
  params.delete("id");
  window.history.pushState({}, "", `?${params.toString()}`);
  window.dispatchEvent(new Event("urlchange"));
}

const providers = ["Yahoo Finance", "Upstox", "Screener"];

const architecture = [
  { icon: <Database className="h-4 w-4" />, title: "Financial Data", copy: "Provider snapshots, ratios, market data, and freshness metadata feed the company view." },
  { icon: <LineChart className="h-4 w-4" />, title: "Technical Signals", copy: "Momentum, volatility, RSI, MACD, trend strength, and risk-sensitive signals are mapped into engine inputs when available." },
  { icon: <BarChart3 className="h-4 w-4" />, title: "Factor Models", copy: "Quality, value, growth, risk, and sector factors create a repeatable ranking structure." },
  { icon: <ShieldCheck className="h-4 w-4" />, title: "Risk Analysis", copy: "Volatility, leverage, cash-flow stress, accounting quality, and confidence indicators are separated from narrative." },
  { icon: <BrainCircuit className="h-4 w-4" />, title: "AI Explanations", copy: "Explanations summarize engine outputs without hiding the measured inputs or provider status." },
];

const methodology = ["Collect Data", "Generate Features", "Generate Factors", "Run Engines", "Produce Rankings"];

export const PublicAboutPage: React.FC = () => {
  return (
    <main className="ss-tv-app ss-tv-stage relative min-h-screen overflow-x-hidden text-[#F7FAFF]">
      <TopNav />
      <MobileNav />

      <section className="relative z-10 mx-auto max-w-7xl px-5 pb-16 pt-28 md:px-8">
        <div className="grid min-h-[72vh] grid-cols-1 items-center gap-10 lg:grid-cols-[0.9fr_1.1fr]">
          <div>
            <div className="mb-6 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-4 py-2 text-[11px] font-bold uppercase tracking-[0.14em] text-[#00FFE0]">
              <Radar className="h-3.5 w-3.5" />
              Transparency architecture
            </div>
            <h1 className="text-5xl font-extrabold leading-[1.02] tracking-normal text-white md:text-7xl">
              Intelligence for Indian Investors
            </h1>
            <p className="mt-7 max-w-2xl text-base leading-8 text-[#B8C4D8] md:text-lg">
              StockStory transforms thousands of financial signals into actionable stock intelligence. The platform is designed to make rankings explainable, source-aware, and useful for serious research.
            </p>
            <div className="mt-9 flex flex-col gap-3 sm:flex-row">
              <button type="button" onClick={() => setPage("signup")} className="ss-premium-button px-7 text-sm">
                Start research
              </button>
              <button type="button" onClick={() => setPage("landing")} className="ss-secondary-button px-7 text-sm">
                Back to home
              </button>
            </div>
          </div>

          <div className="ss-premium-panel p-6">
            <div className="mb-5 flex items-center justify-between">
              <div>
                <div className="text-[10px] font-bold uppercase tracking-[0.14em] text-[#718096]">Trust bar</div>
                <div className="mt-1 text-2xl font-extrabold text-white">Provider-backed intelligence</div>
              </div>
              <LockKeyhole className="h-5 w-5 text-[#00C8FF]" />
            </div>
            <div className="grid grid-cols-1 gap-3 sm:grid-cols-3">
              {providers.map((provider) => (
                <div key={provider} className="rounded-lg border border-white/10 bg-[#05070A]/70 p-4 text-center">
                  <div className="font-mono text-sm font-bold text-white">{provider}</div>
                  <div className="mt-2 text-[10px] uppercase tracking-[0.14em] text-[#718096]">provider</div>
                </div>
              ))}
            </div>
            <div className="mt-5 rounded-lg border border-[#00FFE0]/20 bg-[#00FFE0]/10 p-4 text-sm leading-7 text-[#D6DEEA]">
              Data is provider-driven. Rankings are generated by measured inputs, not paid placements or hidden analyst picks.
            </div>
          </div>
        </div>
      </section>

      <section className="relative z-10 mx-auto max-w-7xl px-5 py-16 md:px-8">
        <div className="mb-8 max-w-3xl">
          <div className="text-[11px] font-bold uppercase tracking-[0.14em] text-[#00C8FF]">Architecture</div>
          <h2 className="mt-3 text-4xl font-extrabold tracking-normal text-white">What the system measures</h2>
        </div>
        <div className="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-5">
          {architecture.map((item) => (
            <div key={item.title} className="ss-premium-panel p-5">
              <div className="mb-7 inline-flex rounded-lg border border-[#00C8FF]/25 bg-[#00C8FF]/10 p-3 text-[#00C8FF]">{item.icon}</div>
              <h3 className="text-lg font-bold text-white">{item.title}</h3>
              <p className="mt-3 text-sm leading-7 text-[#B8C4D8]">{item.copy}</p>
            </div>
          ))}
        </div>
      </section>

      <section className="relative z-10 mx-auto max-w-7xl px-5 py-16 md:px-8">
        <div className="ss-premium-panel p-6 md:p-8">
          <div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end">
            <div>
              <div className="text-[11px] font-bold uppercase tracking-[0.14em] text-[#00FFE0]">Methodology</div>
              <h2 className="mt-3 text-4xl font-extrabold text-white">From raw data to ranking</h2>
            </div>
            <FileSearch className="h-8 w-8 text-[#00FFE0]" />
          </div>
          <div className="grid grid-cols-1 gap-3 md:grid-cols-5">
            {methodology.map((step, index) => (
              <div key={step} className="rounded-lg border border-white/10 bg-[#05070A]/70 p-4">
                <div className="font-mono text-sm font-bold text-[#00C8FF]">{String(index + 1).padStart(2, "0")}</div>
                <div className="mt-8 text-base font-bold text-white">{step}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="relative z-10 mx-auto grid max-w-7xl grid-cols-1 gap-4 px-5 py-16 md:grid-cols-2 md:px-8">
        <div className="ss-premium-panel p-6">
          <div className="text-[11px] font-bold uppercase tracking-[0.14em] text-[#FFC857]">Transparency</div>
          <h2 className="mt-3 text-3xl font-extrabold text-white">What rankings mean</h2>
          <ul className="mt-6 space-y-3 text-sm leading-7 text-[#B8C4D8]">
            <li>StockStory measures business quality, growth, valuation, stability, momentum, risk, and confidence.</li>
            <li>StockStory does not guarantee returns or provide personalised investment advice.</li>
            <li>Rankings change when provider data, factor snapshots, technical inputs, or risk context changes.</li>
            <li>Confidence explains how complete and internally consistent the available data is.</li>
          </ul>
        </div>
        <div className="ss-premium-panel p-6">
          <div className="text-[11px] font-bold uppercase tracking-[0.14em] text-[#00E676]">Real data commitment</div>
          <h2 className="mt-3 text-3xl font-extrabold text-white">No hidden bias layer</h2>
          <ul className="mt-6 space-y-3 text-sm leading-7 text-[#B8C4D8]">
            <li>Provider-driven data is preferred over manual overrides.</li>
            <li>No manual stock picks are inserted into rankings.</li>
            <li>No paid ranking placements are supported.</li>
            <li>Unavailable data should be shown as unavailable, not invented.</li>
          </ul>
        </div>
      </section>

      <section className="relative z-10 mx-auto max-w-7xl px-5 pb-24 md:px-8">
        <div className="ss-premium-panel flex flex-col justify-between gap-6 p-8 md:flex-row md:items-center">
          <div>
            <div className="text-[11px] font-bold uppercase tracking-[0.14em] text-[#00C8FF]">Investor trust</div>
            <h2 className="mt-3 text-3xl font-extrabold text-white">Open the workspace and inspect a company.</h2>
          </div>
          <button type="button" onClick={() => setPage("signup")} className="ss-premium-button px-7 text-sm">
            Create account
            <ArrowRight className="h-4 w-4" />
          </button>
        </div>
      </section>
    </main>
  );
};

export default PublicAboutPage;
